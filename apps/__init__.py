"""ZTOps Django apps.

Each subdirectory is a Django app. Apps are namespaced under `apps.` so
imports are unambiguous ("apps.findings.models") and so the "our code"
boundary is visible from any read of an import line.
"""
